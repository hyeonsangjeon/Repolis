.DS_Store
node_modules/
cloudflare/.wrangler/
cloudflare-taxi/.wrangler/
.wrangler/
# Cloudflare Worker local secrets (wrangler dev) — never commit real keys
.dev.vars
# Local-only notes that record real keys/secrets (kept out of git on purpose)
*.local.md
# Internal design / agent-working docs — kept local, not published to the public repo
# (AGENTS.md is intentionally NOT here — it's the public agent-onboarding spec.)
CHRONOPOLIS_SPEC.md
CLAUDE.md
# Local-only scratch: agent worklogs, screenshots, context-handoff notes — never commit
local.data/
__pycache__/
*.pyc
